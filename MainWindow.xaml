<Window x:Class="FileExplorer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:FileExplorer.Controls"
        xmlns:loc="clr-namespace:FileExplorer.Markup"
        x:Name="RootWindow"
        Title="File Explorer"
        Width="1100"
        Height="680"
        MinWidth="720"
        MinHeight="420"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResizeWithGrip">

    <Window.Resources>
        <!-- Window-only chrome styles (palette comes from active theme dictionary) -->

        <!-- Navigation buttons -->
        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="32" />
            <Setter Property="Height" Value="32" />
            <Setter Property="Margin" Value="0,0,4,0" />
            <Setter Property="Background" Value="{DynamicResource ButtonChromeBrush}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{DynamicResource HoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{DynamicResource ButtonPressedBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Root" Property="Opacity" Value="0.35" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Address / search bar style is in ExplorerTheme.xaml -->

        <!-- Dialog / overlay action buttons -->
        <Style x:Key="DialogActionButtonStyle" TargetType="Button">
            <Setter Property="MinWidth" Value="88" />
            <Setter Property="Height" Value="30" />
            <Setter Property="Padding" Value="14,5" />
            <Setter Property="Background" Value="{DynamicResource DialogButtonBrush}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{DynamicResource DialogButtonHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{DynamicResource SelectedBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Root" Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DialogDangerButtonStyle" TargetType="Button" BasedOn="{StaticResource DialogActionButtonStyle}">
            <Setter Property="Background" Value="{DynamicResource DialogDangerBrush}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{DynamicResource DialogDangerHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{DynamicResource DialogDangerPressedBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Root" Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DialogDeleteCommitButtonStyle" TargetType="Button" BasedOn="{StaticResource DialogActionButtonStyle}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrushDark}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Foreground" Value="#FFFF9A9A" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#44FF6B6B" />
                                <Setter TargetName="Root" Property="BorderBrush" Value="#88FF6B6B" />
                                <Setter Property="Foreground" Value="#FFFFECEC" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{DynamicResource DialogDangerPressedBrush}" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Root" Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Editor chrome -->
        <Style x:Key="EditorActionButtonStyle" TargetType="Button" BasedOn="{StaticResource DialogActionButtonStyle}">
            <Setter Property="MinWidth" Value="72" />
            <Setter Property="Height" Value="28" />
            <Setter Property="Margin" Value="0" />
        </Style>

        <Style x:Key="EditorTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource EditorBackgroundBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
            <Setter Property="CaretBrush" Value="{DynamicResource TextPrimaryBrush}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="14,10" />
            <Setter Property="FontFamily" Value="Consolas" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="AcceptsReturn" Value="True" />
            <Setter Property="AcceptsTab" Value="True" />
            <Setter Property="TextWrapping" Value="NoWrap" />
            <Setter Property="VerticalScrollBarVisibility" Value="Auto" />
            <Setter Property="HorizontalScrollBarVisibility" Value="Auto" />
            <Setter Property="SelectionBrush" Value="{DynamicResource SelectedBrush}" />
        </Style>

        <!-- Title bar chrome buttons -->
        <Style x:Key="TitleBarButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="46" />
            <Setter Property="Height" Value="32" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
            <Setter Property="FontSize" Value="10" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{DynamicResource HoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#33FFFFFF" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CloseButtonStyle" TargetType="Button" BasedOn="{StaticResource TitleBarButtonStyle}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{DynamicResource CloseHoverBrush}" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{DynamicResource DialogDangerPressedBrush}" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Dark minimal scrollbars -->
        <Style x:Key="ScrollBarThumbStyle" TargetType="Thumb">
            <Setter Property="OverridesDefaultStyle" Value="True" />
            <Setter Property="IsTabStop" Value="False" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border Background="{DynamicResource ScrollBarThumbBrush}"
                                CornerRadius="4"
                                Margin="2" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ScrollBarRepeatButtonStyle" TargetType="RepeatButton">
            <Setter Property="OverridesDefaultStyle" Value="True" />
            <Setter Property="IsTabStop" Value="False" />
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border Background="Transparent" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Width" Value="10" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid Background="Transparent">
                            <Track x:Name="PART_Track"
                                   IsDirectionReversed="True"
                                   Focusable="False">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Style="{StaticResource ScrollBarRepeatButtonStyle}"
                                                  Command="ScrollBar.PageUpCommand" />
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Style="{StaticResource ScrollBarRepeatButtonStyle}"
                                                  Command="ScrollBar.PageDownCommand" />
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource ScrollBarThumbStyle}" />
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="Width" Value="Auto" />
                    <Setter Property="Height" Value="10" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="GridViewColumnHeader">
            <Setter Property="Background" Value="{DynamicResource ColumnHeaderBackgroundBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrushDark}" />
            <Setter Property="BorderThickness" Value="0,0,0,1" />
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GridViewColumnHeader">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Dark context menu (fixes default icon-column white stripe) -->
        <Style TargetType="ContextMenu">
            <Setter Property="Background" Value="{DynamicResource ContextMenuBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrushDark}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="4" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContextMenu">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <StackPanel IsItemsHost="True"
                                        Background="Transparent"
                                        KeyboardNavigation.DirectionalNavigation="Cycle" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Separator">
            <Setter Property="OverridesDefaultStyle" Value="True" />
            <Setter Property="Background" Value="{DynamicResource SeparatorBrush}" />
            <Setter Property="Height" Value="1" />
            <Setter Property="Margin" Value="8,4" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Separator">
                        <Border Background="{TemplateBinding Background}"
                                Height="1"
                                SnapsToDevicePixels="True" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="MenuItem">
            <Setter Property="OverridesDefaultStyle" Value="True" />
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Icon" Value="{x:Null}" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="Root"
                                Background="Transparent"
                                CornerRadius="4"
                                SnapsToDevicePixels="True">
                            <Grid Background="Transparent">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="0" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <ContentPresenter Grid.Column="1"
                                                  ContentSource="Header"
                                                  Margin="12,8"
                                                  VerticalAlignment="Center"
                                                  RecognizesAccessKey="True" />
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{DynamicResource HoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- Outer shell with glass background -->
    <Border Background="{DynamicResource GlassBackgroundBrush}"
            BorderBrush="{DynamicResource BorderBrushDark}"
            BorderThickness="1"
            CornerRadius="10"
            SnapsToDevicePixels="True">
        <Border.Effect>
            <DropShadowEffect BlurRadius="24"
                              ShadowDepth="0"
                              Opacity="0.45"
                              Color="Black" />
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="36" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Custom title bar -->
            <Border Grid.Row="0"
                    Background="{DynamicResource TitleBarBackgroundBrush}"
                    CornerRadius="10,10,0,0"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Orientation="Horizontal"
                                VerticalAlignment="Center"
                                Margin="14,0,0,0">
                        <TextBlock Text="&#xE8B7;"
                                   FontFamily="Segoe MDL2 Assets"
                                   FontSize="14"
                                   Foreground="{DynamicResource AccentBrush}"
                                   VerticalAlignment="Center"
                                   Margin="0,0,8,0" />
                        <TextBlock x:Name="AppTitleText"
                                   Text="{loc:Loc UI_AppTitle}"
                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                   FontSize="13"
                                   VerticalAlignment="Center" />
                    </StackPanel>

                    <StackPanel Grid.Column="1"
                                Orientation="Horizontal">
                        <Button x:Name="SettingsButton"
                                Style="{StaticResource TitleBarButtonStyle}"
                                Content="&#xE713;"
                                ToolTip="Settings"
                                Width="40"
                                FontSize="11"
                                Click="SettingsButton_Click" />
                        <Button x:Name="AddPaneButton"
                                Style="{StaticResource TitleBarButtonStyle}"
                                Content="&#xE710;"
                                ToolTip="Add directory pane"
                                Width="40"
                                FontSize="11"
                                Foreground="{DynamicResource AccentBrush}"
                                Click="AddPaneButton_Click" />
                        <Button Style="{StaticResource TitleBarButtonStyle}"
                                Content="&#xE921;"
                                ToolTip="Minimize"
                                Click="MinimizeButton_Click" />
                        <Button x:Name="MaximizeButton"
                                Style="{StaticResource TitleBarButtonStyle}"
                                Content="&#xE922;"
                                ToolTip="Maximize"
                                Click="MaximizeButton_Click" />
                        <Button Style="{StaticResource CloseButtonStyle}"
                                Content="&#xE8BB;"
                                ToolTip="Close"
                                Click="CloseButton_Click" />
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Address bar, main content, dim layer, editor, and name prompt -->
            <Grid Grid.Row="1"
                  Grid.RowSpan="2"
                  x:Name="MainChromeHost"
                  Margin="10,6,10,8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="280" MinWidth="200" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" MinWidth="280" />
                </Grid.ColumnDefinitions>

                    <!-- Navigation tree -->
                    <Border Grid.Row="0"
                            Grid.Column="0"
                            Background="{DynamicResource PanelBackgroundBrush}"
                            BorderBrush="{DynamicResource BorderBrushDark}"
                            BorderThickness="1"
                            CornerRadius="8">
                        <DockPanel LastChildFill="True">
                            <TextBlock DockPanel.Dock="Top"
                                       x:Name="NavigationLabelText"
                                       Text="{loc:Loc UI_Navigation}"
                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                       FontSize="11"
                                       FontWeight="SemiBold"
                                       Margin="12,10,12,6" />
                            <TreeView x:Name="DirectoryTree"
                                      Margin="4,0,4,8"
                                      Background="Transparent"
                                      AlternationCount="0"
                                      AllowDrop="True"
                                      ItemContainerStyle="{DynamicResource ExplorerTreeViewItemStyle}"
                                      SelectedItemChanged="DirectoryTree_SelectedItemChanged"
                                      TreeViewItem.Expanded="DirectoryTreeItem_Expanded"
                                      DragOver="DirectoryTree_DragOver"
                                      Drop="DirectoryTree_Drop">
                                <TreeView.ItemTemplate>
                                    <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                                        <StackPanel Orientation="Horizontal">
                                            <Image Source="{Binding Icon}"
                                                   Width="16"
                                                   Height="16"
                                                   Margin="0,0,6,0"
                                                   VerticalAlignment="Center"
                                                   RenderOptions.BitmapScalingMode="HighQuality" />
                                            <TextBlock Text="{Binding Name}"
                                                       Foreground="{Binding RelativeSource={RelativeSource AncestorType=TreeViewItem}, Path=Foreground}"
                                                       FontSize="13"
                                                       VerticalAlignment="Center" />
                                        </StackPanel>
                                    </HierarchicalDataTemplate>
                                </TreeView.ItemTemplate>
                            </TreeView>
                        </DockPanel>
                    </Border>

                    <GridSplitter Grid.Row="0"
                                  Grid.Column="1"
                                  Width="6"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Stretch"
                                  Background="Transparent"
                                  ShowsPreview="True" />

                    <!-- Dynamic directory pane host -->
                    <Grid Grid.Row="0"
                          Grid.Column="2">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0"
                              Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <Button x:Name="BackNavigationButton"
                                    Grid.Column="0"
                                    Style="{StaticResource NavButtonStyle}"
                                    Content="&#xE72B;"
                                    ToolTip="Back"
                                    IsEnabled="False"
                                    Click="BackNavigationButton_Click" />

                            <Button x:Name="ForwardNavigationButton"
                                    Grid.Column="1"
                                    Style="{StaticResource NavButtonStyle}"
                                    Content="&#xE72A;"
                                    ToolTip="Forward"
                                    IsEnabled="False"
                                    Click="ForwardNavigationButton_Click" />

                            <TextBlock x:Name="SearchingIndicator"
                                       Grid.Column="3"
                                       Text="Searching..."
                                       Foreground="{DynamicResource AccentBrush}"
                                       FontSize="11"
                                       FontWeight="SemiBold"
                                       VerticalAlignment="Center"
                                       Margin="10,0,0,0"
                                       Visibility="Collapsed" />

                            <TextBlock x:Name="ExtractingIndicator"
                                       Grid.Column="3"
                                       Text="Extracting..."
                                       Foreground="{DynamicResource AccentBrush}"
                                       FontSize="11"
                                       FontWeight="SemiBold"
                                       VerticalAlignment="Center"
                                       Margin="10,0,0,0"
                                       Visibility="Collapsed" />
                        </Grid>

                        <Grid x:Name="PaneHostContainer"
                              Grid.Row="1">
                            <controls:DirectoryPaneHost x:Name="DirectoryPaneHost" />
                        </Grid>
                    </Grid>
                <!-- Dimming layer (shared by editor, dialogs) -->
                <Border x:Name="ChromeDimOverlay"
                        Grid.Row="0"
                        Grid.ColumnSpan="3"
                        Background="{DynamicResource DimOverlayBrush}"
                        Opacity="0"
                        Visibility="Collapsed"
                        IsHitTestVisible="False"
                        CornerRadius="8" />

                <!-- Built-in editor overlay -->
                <Border x:Name="EditorOverlay"
                        Grid.Row="0"
                        Grid.Column="2"
                        Visibility="Collapsed"
                        RenderTransformOrigin="0.5,0.5"
                        Margin="4,8,4,8"
                        Background="{DynamicResource PanelBackgroundBrush}"
                        CornerRadius="6"
                        BorderBrush="{DynamicResource BorderBrushDark}"
                        BorderThickness="1">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0"
                                Background="{DynamicResource OverlayHeaderBrush}"
                                CornerRadius="6,6,0,0"
                                Padding="10,8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <StackPanel Orientation="Horizontal"
                                            VerticalAlignment="Center">
                                    <TextBlock Text="&#xE943;"
                                               FontFamily="Segoe MDL2 Assets"
                                               FontSize="14"
                                               Foreground="{DynamicResource AccentBrush}"
                                               VerticalAlignment="Center"
                                               Margin="0,0,8,0" />
                                    <TextBlock x:Name="EditorFileNameText"
                                               Text="Untitled"
                                               Foreground="{DynamicResource TextPrimaryBrush}"
                                               FontSize="13"
                                               FontWeight="SemiBold"
                                               VerticalAlignment="Center"
                                               TextTrimming="CharacterEllipsis" />
                                    <TextBlock x:Name="EditorDirtyIndicator"
                                               Text=" *"
                                               Foreground="{DynamicResource AccentBrush}"
                                               FontSize="13"
                                               FontWeight="SemiBold"
                                               Visibility="Collapsed" />
                                </StackPanel>

                                <Grid Grid.Column="1">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="8" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <Button x:Name="EditorSaveButton"
                                            Grid.Column="0"
                                            Style="{StaticResource EditorActionButtonStyle}"
                                            Content="{loc:Loc UI_Save}"
                                            Click="EditorSaveButton_Click" />
                                    <Button Grid.Column="2"
                                            Style="{StaticResource EditorActionButtonStyle}"
                                            Content="{loc:Loc UI_Close}"
                                            Click="EditorCloseButton_Click" />
                                </Grid>
                            </Grid>
                        </Border>

                        <TextBox x:Name="EditorTextBox"
                                 Grid.Row="1"
                                 Style="{StaticResource EditorTextBoxStyle}"
                                 TextChanged="EditorTextBox_TextChanged"
                                 PreviewKeyDown="EditorTextBox_PreviewKeyDown" />
                    </Grid>
                </Border>

                <!-- Archive viewer overlay -->
                <Border x:Name="ArchiveOverlay"
                        Grid.Row="0"
                        Grid.Column="2"
                        Visibility="Collapsed"
                        RenderTransformOrigin="0.5,0.5"
                        Margin="4,8,4,8"
                        Background="{DynamicResource PanelBackgroundBrush}"
                        CornerRadius="6"
                        BorderBrush="{DynamicResource BorderBrushDark}"
                        BorderThickness="1">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0"
                                Background="{DynamicResource OverlayHeaderBrush}"
                                CornerRadius="6,6,0,0"
                                Padding="10,8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <StackPanel Orientation="Horizontal"
                                            VerticalAlignment="Center">
                                    <TextBlock Text="&#xE8B7;"
                                               FontFamily="Segoe MDL2 Assets"
                                               FontSize="14"
                                               Foreground="{DynamicResource AccentBrush}"
                                               VerticalAlignment="Center"
                                               Margin="0,0,8,0" />
                                    <TextBlock x:Name="ArchiveFileNameText"
                                               Text="archive.zip"
                                               Foreground="{DynamicResource TextPrimaryBrush}"
                                               FontSize="13"
                                               FontWeight="SemiBold"
                                               VerticalAlignment="Center"
                                               TextTrimming="CharacterEllipsis" />
                                </StackPanel>

                                <Grid Grid.Column="1">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="8" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <Button x:Name="ArchiveExtractButton"
                                            Grid.Column="0"
                                            Style="{StaticResource EditorActionButtonStyle}"
                                            Content="{loc:Loc UI_ExtractHere}"
                                            Click="ArchiveExtractButton_Click" />
                                    <Button Grid.Column="2"
                                            Style="{StaticResource EditorActionButtonStyle}"
                                            Content="{loc:Loc UI_Close}"
                                            Click="ArchiveCloseButton_Click" />
                                </Grid>
                            </Grid>
                        </Border>

                        <Grid Grid.Row="1">
                            <ListView x:Name="ArchiveContentsList"
                                      Margin="4,4,4,4"
                                      SelectionMode="Extended"
                                      ItemContainerStyle="{DynamicResource ExplorerListViewItemStyle}"
                                      ContextMenuOpening="ArchiveContentsList_ContextMenuOpening">
                                <ListView.ContextMenu>
                                    <ContextMenu>
                                        <MenuItem x:Name="ArchiveExtractSelectedMenuItem"
                                                  Header="{loc:Loc UI_ExtractSelected}"
                                                  Tag="ExtractSelected"
                                                  Click="ExtractSelectedArchiveEntry_Click" />
                                    </ContextMenu>
                                </ListView.ContextMenu>
                                <ListView.View>
                                    <GridView>
                                        <GridViewColumn x:Name="ArchiveFileNameColumn" Header="File Name" Width="260">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding Name}"
                                                               Foreground="{Binding RelativeSource={RelativeSource AncestorType=ListViewItem}, Path=Foreground}" />
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                        <GridViewColumn x:Name="ArchiveOriginalSizeColumn" Header="Original Size" Width="120">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding OriginalSizeDisplay}"
                                                               Foreground="{Binding RelativeSource={RelativeSource AncestorType=ListViewItem}, Path=Foreground}"
                                                               HorizontalAlignment="Right" />
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                        <GridViewColumn x:Name="ArchiveCompressedSizeColumn" Header="Compressed Size" Width="120">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding CompressedSizeDisplay}"
                                                               Foreground="{Binding RelativeSource={RelativeSource AncestorType=ListViewItem}, Path=Foreground}"
                                                               HorizontalAlignment="Right" />
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                    </GridView>
                                </ListView.View>
                            </ListView>

                            <TextBlock x:Name="ArchiveErrorText"
                                       Text="{loc:Loc UI_ArchiveError}"
                                       Foreground="{DynamicResource ErrorTextBrush}"
                                       FontSize="12"
                                       TextWrapping="Wrap"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       Margin="20"
                                       Visibility="Collapsed" />
                        </Grid>
                    </Grid>
                </Border>

                <!-- Custom name prompt -->
                <Border x:Name="NamePromptOverlay"
                        Grid.Row="0"
                        Grid.ColumnSpan="3"
                        Panel.ZIndex="300"
                        Visibility="Collapsed"
                        Background="Transparent"
                        IsHitTestVisible="True">
                    <Border Width="360"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Center"
                            Background="{DynamicResource GlassBackgroundBrush}"
                            BorderBrush="{DynamicResource BorderBrushDark}"
                            BorderThickness="1"
                            CornerRadius="10"
                            Padding="18"
                            SnapsToDevicePixels="True">
                        <StackPanel Background="Transparent">
                            <TextBlock x:Name="NamePromptTitleText"
                                       Text="New item"
                                       Foreground="{DynamicResource TextPrimaryBrush}"
                                       FontSize="14"
                                       FontWeight="SemiBold"
                                       Margin="0,0,0,12" />
                            <ListView x:Name="GitCommitFilesList"
                                      Height="140"
                                      Margin="0,0,0,12"
                                      Background="{DynamicResource PanelBackgroundBrush}"
                                      BorderBrush="{DynamicResource BorderBrushDark}"
                                      BorderThickness="1"
                                      Visibility="Collapsed"
                                      ItemContainerStyle="{DynamicResource ExplorerListViewItemStyle}">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Ellipse Width="6" Height="6" Margin="4,0,10,0" VerticalAlignment="Center">
                                                <Ellipse.Style>
                                                    <Style TargetType="Ellipse">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Status}" Value="Modified">
                                                                <Setter Property="Fill" Value="{DynamicResource GitModifiedBrush}" />
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Status}" Value="Added">
                                                                <Setter Property="Fill" Value="{DynamicResource GitAddedBrush}" />
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Status}" Value="Untracked">
                                                                <Setter Property="Fill" Value="{DynamicResource GitUntrackedBrush}" />
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Status}" Value="Deleted">
                                                                <Setter Property="Fill" Value="{DynamicResource GitDeletedBrush}" />
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Status}" Value="UpdatedButUnmerged">
                                                                <Setter Property="Fill" Value="{DynamicResource GitConflictBrush}" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Ellipse.Style>
                                            </Ellipse>
                                            <TextBlock Text="{Binding FilePath}" FontSize="11" Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center" />
                                        </StackPanel>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                            <TextBox x:Name="NamePromptTextBox"
                                     Style="{DynamicResource AddressBarTextBoxStyle}"
                                     KeyDown="NamePromptTextBox_KeyDown" />
                            <TextBlock x:Name="NamePromptErrorText"
                                       Foreground="{DynamicResource ErrorTextBrush}"
                                       FontSize="11"
                                       Margin="2,8,0,0"
                                       TextWrapping="Wrap"
                                       Visibility="Collapsed" />
                            <Grid Margin="0,14,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="8" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="8" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Button Grid.Column="2"
                                        x:Name="NamePromptCancelButton"
                                        Style="{StaticResource DialogActionButtonStyle}"
                                        Content="{loc:Loc UI_Cancel}"
                                        Click="NamePromptCancelButton_Click" />
                                <Button Grid.Column="4"
                                        x:Name="NamePromptCreateButton"
                                        Style="{StaticResource DialogActionButtonStyle}"
                                        Content="{loc:Loc UI_Create}"
                                        Click="NamePromptCreateButton_Click" />
                            </Grid>
                        </StackPanel>
                    </Border>
                </Border>

                <!-- AI prompt overlay -->
                <Border x:Name="AiPromptOverlay"
                        Grid.Row="0"
                        Grid.ColumnSpan="3"
                        Panel.ZIndex="300"
                        Visibility="Collapsed"
                        Background="Transparent"
                        IsHitTestVisible="True">
                    <Border x:Name="AiPromptPanel"
                            Width="440"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Center"
                            RenderTransformOrigin="0.5,0.5"
                            Background="{DynamicResource GlassBackgroundBrush}"
                            BorderBrush="{DynamicResource BorderBrushDark}"
                            BorderThickness="1"
                            CornerRadius="10"
                            Padding="18"
                            SnapsToDevicePixels="True">
                        <StackPanel Background="Transparent">
                            <TextBlock x:Name="AiPromptTitleText"
                                       Text="{loc:Loc UI_AiPromptTitle}"
                                       Foreground="{DynamicResource TextPrimaryBrush}"
                                       FontSize="14"
                                       FontWeight="SemiBold"
                                       Margin="0,0,0,8" />
                            <TextBlock x:Name="AiPromptHintText"
                                       Text="{loc:Loc UI_AiPromptHint}"
                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                       FontSize="12"
                                       TextWrapping="Wrap"
                                       Margin="0,0,0,12" />
                            <TextBox x:Name="AiPromptTextBox"
                                     Style="{DynamicResource AddressBarTextBoxStyle}"
                                     MinHeight="72"
                                     AcceptsReturn="True"
                                     TextWrapping="Wrap"
                                     VerticalScrollBarVisibility="Auto"
                                     KeyDown="AiPromptTextBox_KeyDown" />
                            <TextBlock x:Name="AiPromptErrorText"
                                       Foreground="{DynamicResource ErrorTextBrush}"
                                       FontSize="11"
                                       Margin="2,8,0,0"
                                       TextWrapping="Wrap"
                                       Visibility="Collapsed" />
                            <Grid Margin="0,14,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="8" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="8" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Button Grid.Column="2"
                                        x:Name="AiPromptCancelButton"
                                        Style="{StaticResource DialogActionButtonStyle}"
                                        Content="{loc:Loc UI_Cancel}"
                                        Click="AiPromptCancelButton_Click" />
                                <Button Grid.Column="4"
                                        x:Name="AiPromptSubmitButton"
                                        Style="{StaticResource DialogActionButtonStyle}"
                                        Content="{loc:Loc UI_AiSubmit}"
                                        Click="AiPromptSubmitButton_Click" />
                            </Grid>
                        </StackPanel>
                    </Border>
                </Border>

                <!-- AI action preview overlay -->
                <Border x:Name="AiPreviewOverlay"
                        Grid.Row="0"
                        Grid.ColumnSpan="3"
                        Panel.ZIndex="310"
                        Visibility="Collapsed"
                        Background="Transparent"
                        IsHitTestVisible="True">
                    <Border x:Name="AiPreviewPanel"
                            Width="520"
                            MaxHeight="480"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Center"
                            RenderTransformOrigin="0.5,0.5"
                            Background="{DynamicResource GlassBackgroundBrush}"
                            BorderBrush="{DynamicResource BorderBrushDark}"
                            BorderThickness="1"
                            CornerRadius="10"
                            Padding="18"
                            SnapsToDevicePixels="True">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <TextBlock x:Name="AiPreviewTitleText"
                                       Grid.Row="0"
                                       Text="{loc:Loc UI_AiPreviewTitle}"
                                       Foreground="{DynamicResource TextPrimaryBrush}"
                                       FontSize="14"
                                       FontWeight="SemiBold"
                                       Margin="0,0,0,8" />
                            <TextBlock x:Name="AiPreviewHintText"
                                       Grid.Row="1"
                                       Text="{loc:Loc UI_AiPreviewHint}"
                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                       FontSize="12"
                                       TextWrapping="Wrap"
                                       Margin="0,0,0,12" />

                            <Border Grid.Row="2"
                                    Background="{DynamicResource PanelBackgroundBrush}"
                                    BorderBrush="{DynamicResource BorderBrushDark}"
                                    BorderThickness="1"
                                    CornerRadius="8"
                                    Margin="0,0,0,12"
                                    MinHeight="160"
                                    MaxHeight="280">
                                <ListBox x:Name="AiPreviewListBox"
                                         Background="Transparent"
                                         BorderThickness="0"
                                         Foreground="{DynamicResource TextPrimaryBrush}"
                                         FontSize="12"
                                         Padding="4" />
                            </Border>

                            <TextBlock x:Name="AiPreviewErrorText"
                                       Grid.Row="3"
                                       Foreground="{DynamicResource ErrorTextBrush}"
                                       FontSize="11"
                                       TextWrapping="Wrap"
                                       Visibility="Collapsed"
                                       Margin="0,0,0,8" />

                            <Grid Grid.Row="4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="8" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="8" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Button Grid.Column="2"
                                        x:Name="AiPreviewCancelButton"
                                        Style="{StaticResource DialogActionButtonStyle}"
                                        Content="{loc:Loc UI_Cancel}"
                                        Click="AiPreviewCancelButton_Click" />
                                <Button Grid.Column="4"
                                        x:Name="AiPreviewConfirmButton"
                                        Style="{StaticResource DialogActionButtonStyle}"
                                        Content="{loc:Loc UI_AiConfirm}"
                                        Click="AiPreviewConfirmButton_Click" />
                            </Grid>
                        </Grid>
                    </Border>
                </Border>

                <!-- Settings overlay -->
                <Border x:Name="SettingsOverlay"
                        Grid.Row="0"
                        Grid.ColumnSpan="3"
                        Visibility="Collapsed"
                        Background="Transparent"
                        IsHitTestVisible="True">
                    <Border x:Name="SettingsPanel"
                            Width="420"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Center"
                            RenderTransformOrigin="0.5,0.5"
                            Background="{DynamicResource GlassBackgroundBrush}"
                            BorderBrush="{DynamicResource BorderBrushDark}"
                            BorderThickness="1"
                            CornerRadius="10"
                            Padding="18"
                            SnapsToDevicePixels="True">
                        <StackPanel Background="Transparent">
                            <TextBlock x:Name="SettingsTitleText"
                                       Text="{loc:Loc UI_SettingsTitle}"
                                       Foreground="{DynamicResource TextPrimaryBrush}"
                                       FontSize="14"
                                       FontWeight="SemiBold"
                                       Margin="0,0,0,16" />

                            <TextBlock x:Name="SettingsThemeLabel"
                                       Text="{loc:Loc UI_Theme}"
                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                       FontSize="12"
                                       Margin="0,0,0,6" />
                            <ComboBox x:Name="SettingsThemeComboBox"
                                      Margin="0,0,0,14"
                                      SelectionChanged="SettingsThemeComboBox_SelectionChanged" />

                            <TextBlock x:Name="SettingsTimeFormatLabel"
                                       Text="{loc:Loc UI_TimeFormat}"
                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                       FontSize="12"
                                       Margin="0,0,0,6" />
                            <CheckBox x:Name="SettingsTimeFormatCheckBox"
                                      Content="{loc:Loc UI_TimeFormat12h}"
                                      Foreground="{DynamicResource TextPrimaryBrush}"
                                      Margin="0,0,0,14"
                                      Checked="SettingsTimeFormatCheckBox_Changed"
                                      Unchecked="SettingsTimeFormatCheckBox_Changed" />

                            <TextBlock x:Name="SettingsLanguageLabel"
                                       Text="{loc:Loc UI_Language}"
                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                       FontSize="12"
                                       Margin="0,0,0,6" />
                            <ComboBox x:Name="SettingsLanguageComboBox"
                                      Margin="0,0,0,14"
                                      DisplayMemberPath="DisplayName"
                                      SelectionChanged="SettingsLanguageComboBox_SelectionChanged" />

                            <TextBlock x:Name="SettingsAiApiKeyLabel"
                                       Text="{loc:Loc UI_AiOpenRouterApiKey}"
                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                       FontSize="12"
                                       Margin="0,0,0,6" />
                            <PasswordBox x:Name="SettingsAiApiKeyBox"
                                         Margin="0,0,0,4"
                                         Padding="10,6"
                                         Background="{DynamicResource PanelBackgroundBrush}"
                                         Foreground="{DynamicResource TextPrimaryBrush}"
                                         BorderBrush="{DynamicResource BorderBrushDark}"
                                         BorderThickness="1"
                                         PasswordChanged="SettingsAiApiKeyBox_PasswordChanged" />
                            <TextBlock x:Name="SettingsAiApiKeyHint"
                                       Text="{loc:Loc UI_AiApiKeyHint}"
                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                       FontSize="10"
                                       Margin="0,0,0,14" />

                            <TextBlock x:Name="SettingsAiModelLabel"
                                       Text="{loc:Loc UI_AiPreferredModel}"
                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                       FontSize="12"
                                       Margin="0,0,0,6" />
                            <TextBox x:Name="SettingsAiModelTextBox"
                                     Style="{DynamicResource AddressBarTextBoxStyle}"
                                     Margin="0,0,0,18"
                                     LostFocus="SettingsAiModelTextBox_LostFocus" />

                            <Button x:Name="SettingsCloseButton"
                                    Style="{StaticResource DialogActionButtonStyle}"
                                    Content="{loc:Loc UI_Close}"
                                    HorizontalAlignment="Right"
                                    Click="SettingsCloseButton_Click" />
                        </StackPanel>
                    </Border>
                </Border>

                <!-- Git commit history & restore -->
                <Border x:Name="CommitHistoryOverlay"
                        Grid.Row="0"
                        Grid.ColumnSpan="3"
                        Visibility="Collapsed"
                        Background="Transparent"
                        IsHitTestVisible="True">
                    <Grid>
                        <Border x:Name="CommitHistoryPanel"
                                Width="560"
                                MaxHeight="520"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Center"
                                RenderTransformOrigin="0.5,0.5"
                                Background="{DynamicResource GlassBackgroundBrush}"
                                BorderBrush="{DynamicResource BorderBrushDark}"
                                BorderThickness="1"
                                CornerRadius="10"
                                Padding="18"
                                SnapsToDevicePixels="True">
                            <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <TextBlock x:Name="CommitHistoryTitleText"
                                       Grid.Row="0"
                                       Text="{loc:Loc UI_GitVersionHistory}"
                                       Foreground="{DynamicResource TextPrimaryBrush}"
                                       FontSize="14"
                                       FontWeight="SemiBold"
                                       Margin="0,0,0,12" />

                            <Border Grid.Row="1"
                                    Background="{DynamicResource PanelBackgroundBrush}"
                                    BorderBrush="{DynamicResource BorderBrushDark}"
                                    BorderThickness="1"
                                    CornerRadius="8"
                                    Margin="0,0,0,12"
                                    MinHeight="220"
                                    MaxHeight="360">
                                <ListBox x:Name="CommitHistoryListBox"
                                         Background="Transparent"
                                         BorderThickness="0"
                                         Foreground="{DynamicResource TextPrimaryBrush}"
                                         ItemContainerStyle="{DynamicResource CommitHistoryListBoxItemStyle}"
                                         SelectionChanged="CommitHistoryListBox_SelectionChanged">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel>
                                                <TextBlock FontSize="12">
                                                    <Run Text="{Binding Hash}"
                                                         FontWeight="SemiBold"
                                                         FontFamily="Consolas" />
                                                    <Run Text="  ·  " />
                                                    <Run Text="{Binding Date}" />
                                                </TextBlock>
                                                <TextBlock Text="{Binding Message}"
                                                           FontSize="12"
                                                           Margin="0,4,0,0"
                                                           TextWrapping="Wrap"
                                                           Opacity="0.92" />
                                            </StackPanel>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </Border>

                            <TextBlock x:Name="CommitHistoryWarningText"
                                       Grid.Row="2"
                                       Text="{loc:Loc UI_GitRestoreWarning}"
                                       TextWrapping="Wrap"
                                       Foreground="#FFFFB86B"
                                       FontSize="12"
                                       LineHeight="18"
                                       Margin="0,0,0,10"
                                       Visibility="Collapsed" />

                            <TextBlock x:Name="CommitHistoryErrorText"
                                       Grid.Row="3"
                                       Foreground="{DynamicResource ErrorTextBrush}"
                                       FontSize="11"
                                       TextWrapping="Wrap"
                                       Visibility="Collapsed"
                                       Margin="0,0,0,8" />

                            <Grid Grid.Row="4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="8" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="8" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="8" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Button Grid.Column="2"
                                        x:Name="CommitHistoryCancelButton"
                                        Style="{StaticResource DialogActionButtonStyle}"
                                        Content="{loc:Loc UI_Cancel}"
                                        Click="CommitHistoryCancelButton_Click" />
                                <Button Grid.Column="4"
                                        x:Name="CommitHistoryDeleteCommitButton"
                                        Style="{StaticResource DialogDeleteCommitButtonStyle}"
                                        IsEnabled="False"
                                        Click="CommitHistoryDeleteCommitButton_Click">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="&#xE74D;"
                                                   FontFamily="Segoe MDL2 Assets"
                                                   FontSize="11"
                                                   Margin="0,0,6,0"
                                                   VerticalAlignment="Center" />
                                        <TextBlock Text="{loc:Loc UI_GitDeleteCommit}"
                                                   VerticalAlignment="Center" />
                                    </StackPanel>
                                </Button>
                                <Button Grid.Column="6"
                                        x:Name="CommitHistoryRestoreButton"
                                        Style="{StaticResource DialogActionButtonStyle}"
                                        Content="{loc:Loc UI_GitRestoreSelected}"
                                        IsEnabled="False"
                                        Click="CommitHistoryRestoreButton_Click" />
                                <Button Grid.Column="6"
                                        x:Name="CommitHistoryConfirmRestoreButton"
                                        Style="{StaticResource DialogDangerButtonStyle}"
                                        Content="{loc:Loc UI_GitConfirmRestore}"
                                        Visibility="Collapsed"
                                        Click="CommitHistoryConfirmRestoreButton_Click" />
                            </Grid>
                            </Grid>
                        </Border>

                        <Border x:Name="CommitDeleteConfirmOverlay"
                                Visibility="Collapsed"
                                Background="#99000000"
                                SnapsToDevicePixels="True">
                            <Border Width="400"
                                    VerticalAlignment="Center"
                                    HorizontalAlignment="Center"
                                    Background="{DynamicResource GlassBackgroundBrush}"
                                    BorderBrush="{DynamicResource BorderBrushDark}"
                                    BorderThickness="1"
                                    CornerRadius="10"
                                    Padding="18"
                                    SnapsToDevicePixels="True">
                                <StackPanel>
                                    <TextBlock Text="{loc:Loc UI_GitDeleteCommitTitle}"
                                               Foreground="{DynamicResource TextPrimaryBrush}"
                                               FontSize="14"
                                               FontWeight="SemiBold"
                                               Margin="0,0,0,10" />
                                    <TextBlock Text="{loc:Loc UI_GitDeleteCommitMessage}"
                                               TextWrapping="Wrap"
                                               Foreground="{DynamicResource TextSecondaryBrush}"
                                               FontSize="13"
                                               LineHeight="20"
                                               Margin="0,0,0,14" />
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="8" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="8" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <Button Grid.Column="2"
                                                x:Name="CommitDeleteConfirmCancelButton"
                                                Style="{StaticResource DialogActionButtonStyle}"
                                                Content="{loc:Loc UI_Cancel}"
                                                Click="CommitDeleteConfirmCancelButton_Click" />
                                        <Button Grid.Column="4"
                                                x:Name="CommitDeleteConfirmYesButton"
                                                Style="{StaticResource DialogDangerButtonStyle}"
                                                Content="{loc:Loc UI_GitDeleteCommitYes}"
                                                Click="CommitDeleteConfirmYesButton_Click" />
                                    </Grid>
                                </StackPanel>
                            </Border>
                        </Border>
                    </Grid>
                </Border>

                <!-- Delete confirmation dialog -->
                <Border x:Name="DeleteConfirmOverlay"
                        Grid.Row="0"
                        Grid.ColumnSpan="3"
                        Visibility="Collapsed"
                        Background="Transparent"
                        IsHitTestVisible="True">
                    <Border x:Name="DeleteConfirmPanel"
                            Width="400"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Center"
                            RenderTransformOrigin="0.5,0.5"
                            Background="{DynamicResource GlassBackgroundBrush}"
                            BorderBrush="{DynamicResource BorderBrushDark}"
                            BorderThickness="1"
                            CornerRadius="10"
                            Padding="18"
                            SnapsToDevicePixels="True">
                        <StackPanel Background="Transparent">
                            <StackPanel Orientation="Horizontal"
                                        Margin="0,0,0,10">
                                <TextBlock Text="&#xE74D;"
                                           FontFamily="Segoe MDL2 Assets"
                                           FontSize="16"
                                           Foreground="#FFFF6B6B"
                                           VerticalAlignment="Center"
                                           Margin="0,0,10,0" />
                                <TextBlock x:Name="DeleteConfirmTitleText"
                                           Text="{loc:Loc UI_DeleteItem}"
                                           Foreground="{DynamicResource TextPrimaryBrush}"
                                           FontSize="14"
                                           FontWeight="SemiBold"
                                           VerticalAlignment="Center" />
                            </StackPanel>
                            <TextBlock x:Name="DeleteConfirmMessageText"
                                       TextWrapping="Wrap"
                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                       FontSize="13"
                                       LineHeight="20"
                                       Margin="0,0,0,8" />
                            <TextBlock x:Name="DeleteConfirmErrorText"
                                       Foreground="{DynamicResource ErrorTextBrush}"
                                       FontSize="11"
                                       TextWrapping="Wrap"
                                       Visibility="Collapsed"
                                       Margin="0,0,0,4" />
                            <Grid Margin="0,10,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="8" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="8" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Button Grid.Column="2"
                                        x:Name="DeleteConfirmCancelButton"
                                        Style="{StaticResource DialogActionButtonStyle}"
                                        Content="{loc:Loc UI_Cancel}"
                                        Click="DeleteConfirmCancelButton_Click" />
                                <Button Grid.Column="4"
                                        x:Name="DeleteConfirmDeleteButton"
                                        Style="{StaticResource DialogDangerButtonStyle}"
                                        Content="{loc:Loc UI_Delete}"
                                        Click="DeleteConfirmDeleteButton_Click" />
                            </Grid>
                        </StackPanel>
                    </Border>
                </Border>
            </Grid>

            <!-- Status bar -->
            <Border Grid.Row="3"
                    Background="{DynamicResource StatusBarBackgroundBrush}"
                    CornerRadius="0,0,10,10"
                    Padding="14,6">
                <TextBlock x:Name="StatusText"
                           Foreground="{DynamicResource TextSecondaryBrush}"
                           FontSize="11" />
            </Border>
        </Grid>
    </Border>
</Window>
